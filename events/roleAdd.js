const { RichEmbed } = require('discord.js');

class RoleAdd {
    constructor(client) {
        this.client = client;
    };

    /**
     * @param {object} member The member object.
     */
    async run(member) {
    };
};

module.exports = RoleAdd;